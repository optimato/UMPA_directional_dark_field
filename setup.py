from setuptools import setup, find_packages

setup(
    name='UMPA_directional_dark_field',
    version='0.1.0',
    #packages = ['UMPA_directional_dark_field'],
    packages=find_packages(include = ['*']),
)